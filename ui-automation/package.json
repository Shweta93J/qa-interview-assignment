{
  "name": "ui-automation-playwright",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "lint:quick": "node -e \"console.log('No linter configured for this take-home. Keeping it minimal.')\""
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "dotenv": "^16.4.5"
  }
}