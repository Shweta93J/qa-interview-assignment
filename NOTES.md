# Notes

- This repo intentionally does not include any personal credentials or secrets.
- If you want to run with an existing demo user, copy `ui-automation/.env.example` to `ui-automation/.env` and set values locally.
- The final handover is the GitHub repo link. No email attachments needed.
